@using OnlineWallet.ViewModels;
@model TransactionViewModel;

@{
    ViewData["Title"] = "Adicionar ou editar transação";
}

<div class="row justify-content-center">
    <div class="col-md-7">
        <div class="widget p-5" style="background-color:#212b36">
            <form asp-action="AddOrEdit" method="post">
                <div asp-validation-summary="ModelOnly" class="text-danger"></div>
                <div class="mb-3">
                    <div class='e-btn-group custom-rbt-group'>
                        <input type="radio" id="radioIncome" name="Type" value="Income" />
                        <label class="e-btn" for="radioIncome">Renda</label>

                        <input type="radio" id="radioInvestment" name="Type" value="Investment" />
                        <label class="e-btn" for="radioInvestment">Investimento</label>

                        <input type="radio" id="radioExpense" name="Type" value="Expense" />
                        <label class="e-btn" for="radioExpense">Gasto</label>

                    </div>
                </div>
                <div class="mb-3">
                    <ejs-textbox id="title" placeholder="Titulo"
                                 floatLabelType="Always">
                    </ejs-textbox>
                    <span asp-validation-for="Title" class="text-danger fs-6"></span>
                </div>
                <div class="mb-3">
                    <ejs-datepicker id="date"
                                    placeholder="Data"
                                    floatLabelType="Always"
                                    format="dd/MM/yyyy">
                    </ejs-datepicker>
                    <span asp-validation-for="Date" class="text-danger fs-6"></span>
                </div>
                <div class="mb-3">
                    <ejs-numerictextbox id="amount"
                                        placeholder="Valor" floatLabelType="Always"
                                        min=0 format="c0">
                    </ejs-numerictextbox>
                    <span asp-validation-for="Amount" class="text-danger fs-6"></span>
                </div>
                <div class="mb-3">
                    <ejs-textbox id="descriptor" placeholder="Descrição"
                                 floatLabelType="Always" multiline="true">
                    </ejs-textbox>
                </div>
                <ejs-button id="submit" type="submit" content="Gerar Transação" cssClass="e-success"></ejs-button>
            </form>
        </div>
    </div>
</div>

